Extract ALL administrative changes from these records:

{batch_markdown}

**IMPORTANT: Return ONLY valid JSON without any additional text, comments, or explanations. Do not truncate the response.**

Return a JSON array with one object per row. Each object can have multiple changes.